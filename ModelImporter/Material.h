#pragma once


// We will use forward rendering in Importer
// Texture will be diffuse Only
class Material
{
public:
	Material();
	~Material();

private:
	// TODO : Make Shader, Texture
	//std::unique_ptr<class Texture> m_pTexture = nullptr;
	//std::unique_ptr<class Shader> m_pShader = nullptr;


};

